"""Research-only benchmarks — not part of the production benchmark suite.

These modules contain experimental configurations and benchmarks for features
that are not yet production-validated (multi-donor, SemShareKV, scaling tests).
They are excluded from the pip package and are retained here for reference.
"""
